1. they perform the operation specified by the additional operator, then assign the result to the left operand. to combine any arithmetic operator with the assignment operator
1. *=, /=, %=, +=, -=
1. var = var + 5; var += 5; var = var + 50; var += 50;
1. You use a while statement to run a statement repeatedly for as long as some condition is true.
1. the variable that changes each time the loop executes and controls when the loop finishes.
1. it will never end.
1. 3, yes
1. do while loop
1. When you break out of a loop, the loop exits immediately, and execution continues at the fi rst statement that follows the loop.
1. causes the program to perform the next iteration of the loop immediately (after reevaluating the Boolean expression).
